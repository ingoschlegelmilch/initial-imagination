# Asset: Horizontal wooden fence segment
# Category: tile (deviation: decorative overlay, not a tiling base surface)
# Recommended size: 64x64
# Tiling: no
# Palette: inherit — warm browns with cool shadows, matches existing overworld set
# Phaser 3 notes: this.load.image('fence-horizontal', 'assets/tiles/belowPlayer/fence-horizontal.png')
#                 Sits on the `belowPlayer` tilemap layer — needs transparent background
#                 so the underlying ground tile shows through above and below the fence.

PROMPT:
pixel art, horizontal wooden fence segment, two thin parallel horizontal wood rails spanning the full width of the tile from left edge to right edge, one small vertical post in the middle, occupies only the middle vertical band of the tile with empty transparent space above and below, weathered warm brown wood with subtle grain, limited palette, two-tone shading, clean silhouette, slight 3/4 top-down perspective, single subject, centered, transparent background, HD pixel art, game asset, pixel art

STYLE: game asset

NEGATIVE / CAUTIONS:
- The space above and below the fence MUST be transparent — no grass, no dirt, no background fill
- The two rails MUST reach the LEFT and RIGHT edges of the tile so adjacent tiles connect into a continuous fence row
- The fence must NOT span the full HEIGHT — empty space at top and bottom is required
- No text, no watermarks, no drop shadows, no motion lines
- Don't make the fence look like a tall wall or hedge — it's a low, decorative wooden fence

VARIATIONS:
- Damaged segment (broken rail, leaning post)
- Stone fence segment, same silhouette
- Painted white picket variant
